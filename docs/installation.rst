.. highlight:: shell

============
Installation
============


Stable release
--------------

To install Django Amazon Translate, run this command in your terminal:

.. code-block:: console

    $ pip install django_amazon_translate

This is the preferred method to install Django Amazon Translate, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for Django Amazon Translate can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/leepa/django_amazon_translate

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/leepa/django_amazon_translate/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/leepa/django_amazon_translate
.. _tarball: https://github.com/leepa/django_amazon_translate/tarball/master
